name: Build Docker Images

on:
  pull_request:
    branches: [main]
    paths:
      - "agents.json"
      - "docker/**"
      - "scripts/**"
      - "compose.yml"
      - ".github/workflows/build.yml"
      - ".github/workflows/auto-release.yml"
  push:
    branches: [main]
    paths:
      - "agents.json"
      - "docker/**"
      - "scripts/**"
      - "compose.yml"
      - ".github/workflows/build.yml"
      - ".github/workflows/auto-release.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        target:
          - claude
          - gemini
          - opencode
          - devstral
          - auggie
          - copilot
          - codex
          - pi
          - agy
          - tau
          - jcode

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve target metadata
        id: vars
        shell: bash
        run: |
          set -euo pipefail
          target="${{ matrix.target }}"
          context="docker/${target}"
          file="${context}/Dockerfile"
          platforms="linux/amd64,linux/arm64"
          if [[ "${target}" == "devstral" || "${target}" == "gemini" || "${target}" == "agy" ]]; then
            platforms="linux/amd64"
          fi

          echo "context=${context}" >> "${GITHUB_OUTPUT}"
          echo "file=${file}" >> "${GITHUB_OUTPUT}"
          echo "platforms=${platforms}" >> "${GITHUB_OUTPUT}"

      - name: Build image (no push)
        uses: docker/build-push-action@v6
        with:
          context: ${{ steps.vars.outputs.context }}
          file: ${{ steps.vars.outputs.file }}
          platforms: ${{ steps.vars.outputs.platforms }}
          push: false
          provenance: false
          sbom: false
          no-cache: true
          cache-to: type=gha,mode=max,scope=${{ matrix.target }}

      - name: Smoke test image
        shell: bash
        env:
          AGENT_NAMESPACE: vibepod
          IMAGE_TAG: ci-smoke
        run: |
          set -euo pipefail
          node scripts/smoke-agent-images.mjs --targets "${{ matrix.target }}" --build
